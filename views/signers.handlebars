
{{>topimage}}
<div class="container">
    <div class="teaser">
    <div class="teaser-left">
       <h2>These people signed the petition {{#if cityFromUrl}} in {{cityFromUrl}} {{/if}}</h2>
        <div class="signers">
        {{#signers}}
            <p class="signer"> ✅
                {{first}}
                {{last}} {{age}}

            </p>
            {{#if ../cityFromUrl}}

                {{else}}
                                <a href="signers/city/{{city}}">
                {{city}}
                </a>
                {{/if}}
        {{/signers}}
        </div>
    </div>
    <div class="teaser-right">

    <div class="teaser__image"> <img src="images/logo.png" alt="logo" /></div>
    </div>
</div>
</div>

