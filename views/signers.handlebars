{{>topimage}}
<div class="container">
    <div class="box">
        <div class="box-left">
            <h2>These people signed the petition {{#if cityFromUrl}} in
                {{cityFromUrl}} {{/if}}</h2>
            <div class="signers">
                {{#signers}}
                <p class="signer"> ✅
                    {{#if url}}
                    <a class="link-signer" href="{{url}}">
                        {{/if}}
                        {{first}}
                        {{last}}
                        {{#if url}}
                    </a>
                    {{/if}}
                    {{age}}
                {{#if ../cityFromUrl}}
<span></span>
                {{else}}
                <a class="city-link" href="signers/city/{{city}}">{{city}}</a>
                {{/if }}
                </p>

                {{/signers}}
            </div>
        </div>

        <div class="box-right">

            <div class="box__image margin-top-5"> <img src="/images/logo.png"
                    alt="logo" />
            </div>
        </div>
    </div>
</div>
